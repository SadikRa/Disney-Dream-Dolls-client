const Blogs = () => {
  return (
    <div>
      <h1 className="text-4xl">Question:What is an access token and refresh token? How do they work and where should we store them on the client-side?</h1>
      <p>
        Answer: An access token and refresh token are both used in the context
        of authentication and authorization mechanisms, such as OAuth, to ensure
        secure communication between a client (typically a web or mobile
        application) and a server.
      </p>
    </div>
  );
};

export default Blogs;
